#include "parser.h"

/* Extrait date, heure, type, utilisateur et IP depuis une ligne de log. */
int parse_log_line(const char *line, LogEntry *entry) {
    (void)line;
    (void)entry;

    /* TODO : parser la ligne et remplir entry. */
    return 0;
}
